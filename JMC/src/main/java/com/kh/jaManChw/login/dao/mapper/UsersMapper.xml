<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.kh.jaManChw.login.dao.face.UsersDao">
  
  <select id="selectCntByUserIdPw" resultType="int" parameterType="com.kh.jaManChw.dto.Users">
  	SELECT count(*)
  	FROM USERS
  	WHERE USER_ID=#{userId} AND USER_PW=#{userPw}
  </select>
  
  <select id="selectByuserRole" resultType="String" parameterType="com.kh.jaManChw.dto.Users">
  	SELECT ROLE
  	FROM USERS
  	WHERE USER_ID=#{userId} AND USER_PW=#{userPw}
  </select>
  
  <select id="selectCntByUserId" resultType="int" parameterType="com.kh.jaManChw.dto.Users">
  	SELECT count(*)
  	FROM USERS
  	WHERE USER_ID=#{userId}
  
  </select>
  
<!--   <insert id="insertUsers" parameterType="com.kh.jaManChw.dto.Users"> -->
<!--   	INSERT INTO USERS(USERNO,USER_ID,USER_PW,USER_NICK,USER_NAME,BIRTH,GENDER,EMAIL,PHONE,ADDR1,ADDR2,ADDR3,GRADE,WARN_COUNT,PROFIL_IMG_URL,STATUS,ROLE,PLATFORM_OPTION) -->
<!--   	VALUES (USERS_SEQ.NEXTVAL,#{userId},#{userPw},#{userId},'NICK',#{birth},#{gender},#{email},#{phone},#{addr1},#{addr2},#{addr3},1,0,'url',DEFAULT,DEFAULT,'origin')  -->
<!--   </insert> -->
  
  
  </mapper>